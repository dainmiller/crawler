describe CiaCrawler, type: :request do

  it "should start with the reading room URL" do
    expect(true).to_be true
  end

  it "should search for the first search result in the default terms file" do
    it "should return all search results, and then iterate through them all to get to the item page" do
      it "should store and save the PDF it finds" do
      end
    end
  end
end
describe RunCrawler, type: :model do

  it "should start all crawlers when #.start_all is called" do
  end

  it "should start crawler one when #.start_cia is called" do
  end

  it "should start crawler two when #.start_fbi is called" do
  end
  
end